#ifndef MAIN_H
#define MAIN_H

/* standard header files */
#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>
#include <limits.h>
#include <string.h>

/* prototypes */
int _printf(const char *format, ...);
int print_string(char *input_str);

#endif /* MAIN_H */
